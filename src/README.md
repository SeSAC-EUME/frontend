# 프로젝트 구조

이 프로젝트는 **관리자(Admin)**와 **사용자(User)** 앱을 완전히 분리한 구조입니다.

## 📁 디렉토리 구조

```
src/
├── admin/                    # 관리자 앱 (완전히 독립)
│   ├── pages/               # 관리자 페이지
│   │   ├── Login.jsx
│   │   ├── Dashboard.jsx
│   │   ├── Users.jsx
│   │   └── Emergency.jsx
│   ├── components/          # 관리자 전용 컴포넌트
│   ├── hooks/               # 관리자 전용 훅
│   ├── utils/               # 관리자 전용 유틸
│   ├── styles/              # 관리자 전용 스타일
│   │   ├── admin.css
│   │   └── admin-responsive.css
│   ├── assets/              # 관리자 전용 에셋
│   └── routes.jsx           # 관리자 라우팅
│
├── user/                     # 사용자 앱 (완전히 독립)
│   ├── pages/               # 사용자 페이지
│   │   ├── Splash.jsx
│   │   ├── Login.jsx
│   │   ├── OAuth2Redirect.jsx
│   │   ├── Onboarding1-4.jsx
│   │   ├── Home.jsx
│   │   └── Settings.jsx
│   ├── components/          # 사용자 전용 컴포넌트
│   ├── hooks/               # 사용자 전용 훅
│   ├── utils/               # 사용자 전용 유틸
│   ├── styles/              # 사용자 전용 스타일
│   │   ├── user.css
│   │   └── user-responsive.css
│   ├── assets/              # 사용자 전용 에셋
│   └── routes.jsx           # 사용자 라우팅
│
├── shared/                   # 공통 모듈
│   ├── api/                 # API 클라이언트 (axios, config)
│   ├── components/          # 공통 컴포넌트
│   ├── contexts/            # 공통 Context (ThemeContext 등)
│   └── assets/              # 공통 에셋 (로고 등)
│
├── routes.jsx               # 메인 라우터 (admin/user 통합)
├── main.jsx                 # 앱 엔트리포인트
└── index.css                # 전역 스타일
```

## 🎯 Import 경로

### Alias 설정 (vite.config.js)

```javascript
'@'       => './src'
'@admin'  => './src/admin'
'@user'   => './src/user'
'@shared' => './src/shared'
```

### 사용 예시

```javascript
// Admin 페이지에서
import '../styles/admin.css'           // Admin 스타일
import logo from '@shared/assets/logo.png'  // 공통 로고
import { API_ENDPOINTS } from '@shared/api/config'  // API 설정

// User 페이지에서
import '../styles/user.css'            // User 스타일
import { ThemeProvider } from '@shared/contexts/ThemeContext'  // 공통 Context
```

## 🚀 개발 가이드

### Admin 개발자
- `src/admin/` 폴더만 작업
- Admin 전용 컴포넌트는 `admin/components/`에 생성
- 공통 기능이 필요하면 `shared/`에 추가

### User 개발자
- `src/user/` 폴더만 작업
- User 전용 컴포넌트는 `user/components/`에 생성
- 공통 기능이 필요하면 `shared/`에 추가

### 새 페이지 추가하기

1. **Admin 페이지 추가**
   ```bash
   # 1. 페이지 생성
   src/admin/pages/NewPage.jsx

   # 2. 라우트 등록
   src/admin/routes.jsx
   ```

2. **User 페이지 추가**
   ```bash
   # 1. 페이지 생성
   src/user/pages/NewPage.jsx

   # 2. 라우트 등록
   src/user/routes.jsx
   ```

## ✨ 이 구조의 장점

1. **독립적 개발**: Admin과 User 개발자가 충돌 없이 작업 가능
2. **명확한 책임**: 각 폴더의 역할이 명확함
3. **코드 분리**: Admin 코드가 User 번들에 포함되지 않음
4. **쉬운 확장**: 새로운 앱(모바일, 슈퍼관리자 등) 추가 용이
5. **유지보수**: 파일 찾기 쉽고, 의존성 관리 명확

## 🔧 마이그레이션 완료

- ✅ 디렉토리 구조 재구성
- ✅ Admin/User 라우트 분리
- ✅ Import 경로 수정
- ✅ Vite alias 설정
- ✅ 빌드 테스트 완료
